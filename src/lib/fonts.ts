import { Lora, Rock_Salt } from "next/font/google";

export const lora = Lora({
  variable: "--font-lora",
  subsets: ["latin"],
  weight: ["400"],
});

export const rockSalt = Rock_Salt({
  subsets: ["latin"],
  weight: ["400"],
});
